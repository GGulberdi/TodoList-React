import TodoList from './components/TodoList';

function App () {
    return (
      <div>
        <h1 style={{color:'greenyellow', fontSize: 50}}> Todo List</h1>
        <TodoList />
      </div>
    );
}

export default App;
